import { ChangeRatingPipe } from './change-rating.pipe';

describe('ChangeRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
